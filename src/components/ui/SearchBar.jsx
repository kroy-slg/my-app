import React from "react";

const SearchBar = () => {
    return (
        <div className="top-header-left">
            <input
                type="text"
                className="top-search"
                placeholder="Search..."
            />
        </div>
    );
};

export default SearchBar;
